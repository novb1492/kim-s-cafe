function setup() {
    var framerate=60;
    frameRate(framerate);
    let canvas=createCanvas(displayWidth,windowHeight);
    canvas.parent('canvas');
 
  }

function draw() 
{  
     background('gray'); 
}